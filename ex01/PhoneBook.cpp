#include "PhoneBook.hpp"

Contact    add(PhoneBook phb)
{
    std::string firstName;
    std::string lastName;
    std::string nickname;
    std::string secret;

    scanf("%[^\n]s", firstName);
    scanf("%[^\n]s", lastName);
    scanf("%[^\n]s", nickname);
    scanf("%[^\n]s", secret);
		if (!firstName || !lastName || !nickname || !secret)
			return (NULL);
    // phb.add(Contact(firstName, lastName, nickname, secret));
    if (len == 9)

}

Contact    search(PhoneBook phb)
{
	for(int i; i < 8; i++)
		if (std::cout << c.index << c.firstName << c.lastName << c.nickname << str::endl)

}

void add(Contact cnt)
{
        ;

}

