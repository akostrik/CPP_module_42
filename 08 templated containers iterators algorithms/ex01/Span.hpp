// une fonction permet d’ajouter plusieurs nombres en un appel

// member functions use STL algorithms to find their result as much as possible
// Finding the shortest span can't be done only by subtracting the two lowest numbers (take a closer look at the subject example)